import 'package:flutter/material.dart';

class HowIAmPage extends StatelessWidget {
  const HowIAmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Who I Am'),
      ),
      body: Container(),
    );
  }
}
